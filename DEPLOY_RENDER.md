# 🚀 Деплой на Render.com

## Что сделано автоматически

✅ Создан `Dockerfile` с поддержкой Playwright  
✅ Добавлен `playwright==1.40.0` в `requirements.txt`  
✅ Обновлён `app.py`:
- Flask запускается на `0.0.0.0` с портом из переменной `PORT`
- Playwright запускается с флагами `--no-sandbox` для Docker
- РМЭБ использует Playwright для получения прямых ссылок

---

## 📋 Инструкция по деплою

### 1. **Закоммить изменения в Git**

```bash
git add .
git commit -m "Add Playwright support for Render"
git push
```

### 2. **Настроить Render Dashboard**

1. Открой твой сервис на [Render Dashboard](https://dashboard.render.com)
2. Перейди в **Settings** → **Build & Deploy**
3. Измени настройки:
   - **Environment**: `Docker`
   - **Build Command**: оставь пустым
   - **Start Command**: оставь пустым (использует `CMD` из Dockerfile)
4. **Save Changes**

### 3. **Задеплоить**

В разделе **Manual Deploy** нажми:
- **Deploy latest commit**

⏱️ **Первый деплой займёт 5-10 минут** (установка браузера Chromium)

---

## ⚙️ После деплоя

### Проверка логов

После деплоя проверь логи на Render:
- **Logs** → смотри на строки `[DEBUG] RMЭБ: запускаем Playwright...`

### Если всё работает

Должен появиться лог:
```
[DEBUG] RMЭБ: запускаем Playwright для получения прямых ссылок
[DEBUG] RMЭБ Playwright: найден результат 1: Название книги -> https://rmebrk.kz/book/123456
```

---

## 🐛 Если ошибки

### 1. **Out of memory**
- Апгрейд до **Starter Plan** ($7/месяц) для 512MB RAM

### 2. **Playwright timeout**
- Увеличь timeout в `app.py`:
  ```python
  page.set_default_timeout(30000)  # 30 секунд
  ```

### 3. **Chrome crashed**
- Уже исправлено флагами `--no-sandbox` в коде

---

## 💡 Полезные команды

Проверить статус:
```bash
curl https://your-app.onrender.com/
```

Посмотреть логи в реальном времени:
- Render Dashboard → **Logs** → включи **Auto-scroll**

---

## ✅ Готово!

После деплоя РМЭБ будет работать с прямыми ссылками через Playwright! 🎉

